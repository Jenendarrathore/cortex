# FastAPI Agent Service Plan

- [x] Define FastAPI project structure (package layout, config handling, dependencies).
  - [x] Decide on folder layout (`fastapi_app/app`, `fastapi_app/tests`, `fastapi_app/requirements.txt`).
  - [x] Choose config strategy (Pydantic `Settings`, `.env` support).
  - [x] List core dependencies (FastAPI, Uvicorn, Pydantic, SQLAlchemy, async DB driver).
- [x] Scaffold the FastAPI app with a health endpoint and configuration loader.
  - [x] Create `app/main.py` with FastAPI instance, startup logging, and health route.
  - [x] Add `app/config.py` for settings + environment variable loading.
- [x] Add an embeddings/retrieval placeholder endpoint that will later query the knowledge-base vectors.
  - [x] Define request/response schemas (query text, optional collections filter).
  - [x] Implement stub handler that returns mock results while embedding pipeline is wired.
- [x] Document how to run the service locally (uvicorn command, environment variables) and describe next integration steps.
  - [x] Write `README_FASTAPI.md` with setup, run, and future work instructions.
  - [x] Note how checksum + index metadata feed into retrieval layer.
- [x] Prepare for conversation/session storage via SQLAlchemy.
  - [x] Design a `Session` model with enterprise fields (session_id, tenant_id, primary_user_id, status, last_activity_at, ttl, secure metadata JSONB, encryption flags).
  - [x] Support participant roster / access control per session (many-to-many table for users/clients with roles).
  - [x] Set up database engine creation, migrations placeholder, and dependency injection for request handlers.
  - [x] Plan retention + archival strategy (soft delete flag, purge job hooks).
- [x] Design session event tracking.
  - [x] Create an `Event` model linked to `Session` capturing role, event_type (`user_message`, `assistant_reply`, `tool_call`, `tool_result`, `system_notice`), payload, tool invocation info, latency, embeddings/context ids, attachments metadata.
  - [x] Track per-event status (`pending`, `in_progress`, `completed`, `errored`) and error details for UI visibility.
  - [x] Add event indexing for search (e.g., text vector column or reference to vector store ids).
  - [x] Expose endpoints to append/read/filter events with pagination, redaction rules, per-type filters, and export capability for audits. Include push hooks/WebSocket notifications so UIs can reflect live agent activity.
- [x] Configure cross-client access patterns.
  - [x] Add CORS middleware with configurable allowlist for browser apps.
  - [x] Plan WebSocket or SSE endpoint for real-time streaming to web clients.
- [x] Implement observability + rate limiting.
  - [x] Configure structured logging and OpenAPI tags.
  - [x] Add rate limiter middleware or document gateway enforcement (per client quotas).
- [x] Deployment + operations readiness.
  - [x] Create a Dockerfile / containerization plan.
  - [x] Define environment-specific configs (dev/stage/prod) and secret management approach.
- [x] Embedding + retrieval pipeline integration.
  - [x] Build a chunker that reads `knowledge-base/index.json`, respects collections, and emits chunk metadata/hashes.
  - [x] Implement an embedding service wrapper (batching, retries, logging) and persist vectors to the chosen store (namespace per collection).
  - [x] Expose an internal API/module for retrieval that FastAPI endpoints can call (query + optional collection filter → top chunks with metadata/citations).
  - [x] Trigger checksum-driven refresh at application startup and log outcomes (monitoring hooks can evolve later).
- [ ] Add authentication + client management.
  - [ ] Decide on auth scheme (API keys vs OAuth2) and create dependency helpers.
  - [ ] Create a clients table/seed data so each downstream system gets scoped access.
